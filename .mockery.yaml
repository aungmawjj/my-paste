with-expecter: True
inpackage: True
dir: "{{.InterfaceDir}}"
mockname: "Mock{{.InterfaceName}}"
outpkg: "{{.PackageName}}"
filename: "mock_{{.InterfaceName}}_gen_test.go"

packages:
  github.com/aungmawjj/my-paste/mypaste:
    interfaces:
      IdTokenValidator:
      GoogleSignInValidator:
      EchoContext:
      EchoLogger:
